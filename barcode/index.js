/**
 * @format
 */

import {AppRegistry} from 'react-native';
import ScanScreen from './components/ScannerScreen';
import {name as appName} from './app.json';

AppRegistry.registerComponent(appName, () => ScanScreen);
